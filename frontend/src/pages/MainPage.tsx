/**
 * Phase 0 placeholder for the mockup's Main View (ui-example L716) —
 * Repositories / Branches on the left, Files / Changes / Journal on the right.
 * Ported in Phase 4 (PLAN.md §7).
 */
export function MainPage() {
  return <div>Main View — Phase 4</div>;
}
